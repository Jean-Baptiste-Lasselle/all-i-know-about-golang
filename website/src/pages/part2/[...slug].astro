---
import ChapterLayout from '../../layouts/base.astro';

import { getCollection } from 'astro:content';
// 1. Generate a new path for every collection entry
export async function getStaticPaths() {
  const chaptersEntries = await getCollection('part2-chapters');
  return chaptersEntries.map(entry => ({
    params: { slug: entry.slug }, props: { entry },
  }));
}
// 2. For your template, you can get the entry directly from the prop
const { entry } = Astro.props;
const { Content } = await entry.render();
---
<ChapterLayout title="Home Page">
    <h1>{entry.data.title}</h1>
    <article>
        <p>My first Chapter, wrapped in a layout!</p>
    </article>
    <Content />
</ChapterLayout>



